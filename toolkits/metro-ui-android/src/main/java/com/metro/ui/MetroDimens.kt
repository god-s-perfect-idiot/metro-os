package com.metro.ui

import androidx.compose.ui.unit.dp

/**
 * Shared layout tokens for Metro page chrome.
 *
 * Screen side margins match the launcher Start / app-list inset (12dp), not the older
 * 24dp list-margin token.
 */
object MetroDimens {
    /** Left/right inset for page headers, lists, and body content. */
    val ScreenHorizontalMargin = 12.dp
}
