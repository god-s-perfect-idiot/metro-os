# Agent instructions — Messaging (`com.metro.messaging`)

**Tier 2** | SMS/MMS thread list. Reference: `references/images/`.

Thread list + conversation pages. WP8.1 message bubbles: accent received (left + top-left tail), darkened accent sent (right + bottom-right tail), light-gray outgoing-shaped composer. Requires SMS permissions.

Verify: `../../scripts/verify-app.sh messaging`
