# Agent instructions — Files (`com.metro.files`)

**Tier 2** | File explorer with pivot filters. Authoritative: `references/guides/blueprint.md`.

Pivots: all, documents, music, pictures, videos. `MetroListItem` for entries. All-files access for browse; open via `FileProvider` + `ACTION_VIEW`.

Verify: `../../scripts/verify-app.sh files`
