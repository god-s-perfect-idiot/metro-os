# Agent instructions — Mail (`com.metro.mail`)

**Tier 2** | Linked inboxes, conversation view. Reference: `references/images/`.

Pivot for folders; conversation list with `MetroListItem`. IMAP stub acceptable v1. No Material swipe actions — use app bar.

Verify: `../../scripts/verify-app.sh mail`
