# Agent instructions — Phone (`com.metro.dialer`)

**Tier 2** | WP 8.1 Phone app. Reference: `references/guides/blueprint.md`.

`MetroPivot` (`history` | `speed dial`). Dial pad is a separate full page. In-call is full-screen. Read call log via `CallLog.Calls`.

Verify: `../../scripts/verify-app.sh dialer`
