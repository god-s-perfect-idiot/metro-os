<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <queries>
        <package android:name="com.metro.launcher" />
        <package android:name="com.metro.dialer" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MetroPeople">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.MetroPeople">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="metro"
                    android:host="people"
                    android:pathPrefix="/contact" />
            </intent-filter>
        </activity>

        <provider
            android:name=".tiles.PeopleTileProvider"
            android:authorities="com.metro.people.tiles"
            android:exported="true"
            android:grantUriPermissions="true">
            <meta-data
                android:name="com.metro.tile.provider"
                android:value="true" />
        </provider>
    </application>
</manifest>
